post_install() {
	dkms install sensorium/@VERSION@ --no-depmod >/dev/null 2>&1 || true
}

post_upgrade() {
	post_install
}

pre_remove() {
	dkms remove sensorium/@VERSION@ --all >/dev/null 2>&1 || true
}
