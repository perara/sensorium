# Copy to .env.remote and set the droplet IP when ready.
export REMOTE_HOST=203.0.113.10
export REMOTE_USER=root
export REMOTE_PORT=22
export REMOTE_REPO_DIR=/opt/sensorium
export REMOTE_SSH_OPTS="-o StrictHostKeyChecking=accept-new"
